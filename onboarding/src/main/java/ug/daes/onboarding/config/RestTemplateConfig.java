package ug.daes.onboarding.config;

import org.springframework.context.annotation.Configuration;

/**
 * RestTemplate bean has been consolidated into PlatformHttpConfig (common module).
 * This class is kept as a placeholder to maintain the package structure.
 */
@Configuration("onboardingRestTemplateConfig")
public class RestTemplateConfig {
    // RestTemplate bean removed — use the shared bean from PlatformHttpConfig (common module).
}
